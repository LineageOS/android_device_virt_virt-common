type setup_sound_card_mixer, domain;
type setup_sound_card_mixer_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(setup_sound_card_mixer);

r_dir_file(setup_sound_card_mixer, proc_asound);

allow setup_sound_card_mixer audio_device:chr_file rw_file_perms;
